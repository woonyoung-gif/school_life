<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>즐거운 학교 생활</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Jua&display=swap');

        :root {
            --font-main: 'Jua', sans-serif;
            --glass-bg: rgba(255, 255, 255, 0.65);
            --glass-border: rgba(255, 255, 255, 0.4);
            --glass-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.15);

            /* Gradient Colors */
            --blob-1: #FFD93D;
            --blob-2: #FF6B6B;
            --blob-3: #4D96FF;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: var(--font-main);
            background: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            overflow-x: hidden;
            position: relative;
        }

        /* Floating Background Shapes */
        .bg-shape {
            position: absolute;
            border-radius: 50%;
            filter: blur(80px);
            z-index: -1;
            opacity: 0.6;
            animation: floatBG 20s infinite alternate;
        }

        .shape-1 {
            width: 500px;
            height: 500px;
            background: var(--blob-1);
            top: -100px;
            left: -100px;
        }

        .shape-2 {
            width: 400px;
            height: 400px;
            background: var(--blob-2);
            bottom: -50px;
            right: -50px;
            animation-duration: 25s;
        }

        .shape-3 {
            width: 300px;
            height: 300px;
            background: var(--blob-3);
            bottom: 20%;
            left: 20%;
            animation-duration: 15s;
        }

        header {
            text-align: center;
            margin-bottom: 3rem;
            z-index: 10;
            position: relative;
        }

        h1 {
            font-size: 3.5rem;
            color: #2d3748;
            margin-bottom: 0.5rem;
            /* Stylish Text Gradient */
            background: linear-gradient(45deg, #FF6B6B, #4D96FF);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.1));
        }

        .subtitle {
            font-size: 1.6rem;
            color: #555;
            font-weight: normal;
            letter-spacing: 1px;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 2.5rem;
            width: 90%;
            max-width: 900px;
            padding: 1rem;
            z-index: 10;
        }

        /* Glassmorphism Cards */
        .card {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 30px;
            padding: 2.5rem;
            text-align: center;
            text-decoration: none;
            color: #333;
            box-shadow: var(--glass-shadow);
            transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
            /* Spring effect */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        /* Glossy highlight on top */
        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 50%;
            background: linear-gradient(to bottom, rgba(255, 255, 255, 0.4), transparent);
            border-radius: 30px 30px 0 0;
            pointer-events: none;
        }

        .card:hover {
            transform: translateY(-15px) scale(1.03);
            background: rgba(255, 255, 255, 0.85);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .card-icon {
            font-size: 4.5rem;
            margin-bottom: 1rem;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
            transition: transform 0.3s ease;
        }

        .card:hover .card-icon {
            transform: scale(1.2) rotate(5deg);
        }

        .card-title {
            font-size: 2.2rem;
            font-weight: normal;
            color: #4a5568;
        }

        /* Specific Card Colors (Subtle borders/glows) */
        .card-hanja {
            border-bottom: 6px solid #FF6B6B;
        }

        .card-school {
            border-bottom: 6px solid #4D96FF;
        }

        .card-typing {
            border-bottom: 6px solid #FFD93D;
        }

        .card-zoo {
            border-bottom: 6px solid #6BCB77;
        }

        /* Animations */
        @keyframes floatBG {
            0% {
                transform: translate(0, 0) rotate(0deg);
            }

            100% {
                transform: translate(50px, 50px) rotate(10deg);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            h1 {
                font-size: 2.5rem;
                padding: 0 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="bg-shape shape-1"></div>
    <div class="bg-shape shape-2"></div>
    <div class="bg-shape shape-3"></div>

    <header>
        <h1>운영샘과 함께하는 즐거운 학교 생활</h1>
        <p class="subtitle">오늘도 신나게 놀아볼까요?</p>
    </header>

    <div class="container">
        <!-- 1. Hanja Memory -->
        <a href="hanja_memory.html" class="card card-hanja">
            <div class="card-icon">🎴</div>
            <h2 class="card-title">한자 기억력</h2>
        </a>

        <!-- 2. School Habit -->
        <a href="school_habit_game.html" class="card card-school">
            <div class="card-icon">🏫</div>
            <h2 class="card-title">학교 생활 습관</h2>
        </a>

        <!-- 3. Typing Game -->
        <a href="typing_game.html" class="card card-typing">
            <div class="card-icon">⌨️</div>
            <h2 class="card-title">타자 연습</h2>
        </a>

        <!-- 4. Zoo Friend -->
        <a href="zoo_friend.html" class="card card-zoo">
            <div class="card-icon">🦁</div>
            <h2 class="card-title">동물 친구 찾기</h2>
        </a>
    </div>
</body>

</html>
